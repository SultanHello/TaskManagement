spring:
  application:
    name: task_management
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://postgres_bd:5432/users
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
server:
  port: 9001